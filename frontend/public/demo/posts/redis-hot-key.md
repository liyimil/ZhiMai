# Redis 热点 Key 的发现与治理

热点 Key 的本质是大量请求集中到同一份数据或同一个分片。治理前先确认热点来自正常业务增长，还是缓存穿透、重试风暴等异常流量。

## 处理路径

1. 用监控定位命令、Key 和时间窗口。
2. 检查过期策略，避免大量 Key 同时失效。
3. 对只读热点使用本地缓存或副本分散流量。
4. 对大 Key 拆分数据结构，控制单次读写量。
5. 为降级和限流准备明确阈值。

不要只增加 Redis 机器，先消除不必要的重复访问。
